#include<stdio.h>
#include<string.h>
int main()
{
	int la,lb;
	char pa[12],pb[12];
	for(int i=0;i<=12;i++)
	{
		scanf("%c",&pa[i]);
		if(pa[i]==' ')
		{
			pa[i]='\0';
			break;
		}
	}
	scanf("%s",&pb);
	la=strlen(pa)-1;
	lb=strlen(pb)-1;
	int shuma(char x[],char y[]);
	return 0;
}
int shuma(char x[],char y[])
{
	
}
